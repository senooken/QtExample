{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Hello",
            "type": "shell",
            "command": "qmake",
            "args": [
                "&&", "make", "&&", "${workspaceFolder}/*.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}