{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Hello",
            "type": "shell",
            "command": "qmake",
            "args": [
                "main.pro", "&&", "make", "&&", "${workspaceFolder}/main.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}